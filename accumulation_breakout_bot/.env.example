# Optional MT5 credentials (leave config/config.yaml mt5 section empty and the
# bot will use the already-logged-in terminal instead).
# Copy to .env and fill in if you prefer environment-based credentials.
MT5_LOGIN=
MT5_PASSWORD=
MT5_SERVER=
MT5_PATH=
